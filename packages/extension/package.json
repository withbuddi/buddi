{
  "name": "@buddi/extension", "version": "0.1.0", "private": true, "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": { "@types/node": "^22.10.2", "esbuild": "^0.21.5", "jsdom": "^25.0.1", "typescript": "^5.6.3", "vitest": "^2.1.8" },
  "files": ["dist"]
}
